# pchtxt_to_ips
switch的.pchtxt补丁转.ips工具，用于生成 Atmosphere exefs_patches 补丁文件
A tool for converting Nintendo Switch `.pchtxt` patches to `.ips` files, used to generate Atmosphere `exefs_patches` patch files.
